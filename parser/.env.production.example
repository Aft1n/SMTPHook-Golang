# .env.production.example for parser service

# Polling interval in seconds
POLL_INTERVAL=5

# Webhook endpoint to forward parsed emails
WEBHOOK_URL=https://your.production-api.local/email

# Optional: path to local maildir or inbox if not using SMTP
MAIL_DIR=/mail/inbox
